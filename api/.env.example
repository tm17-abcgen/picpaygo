# OpenRouter AI
OPENROUTER_API_KEY=sk-or-v1-...
OPENROUTER_API_URL=https://openrouter.ai/api/v1/chat/completions
OPENROUTER_MODEL=google/gemini-3-pro-image-preview

# Worker config
JOB_WORKERS=2
JOB_TIMEOUT_SECONDS=120

# CORS
CORS_ORIGINS=http://localhost:8082

# Session
SESSION_TTL_HOURS=168
FREE_CREDITS=1

# MinIO (object storage)
# Images are served via the API proxy endpoint (/api/images/{bucket}/{key})
# No special DNS configuration required.
MINIO_ENDPOINT=minio:9000
MINIO_USE_HTTPS=false

# Stripe (payment processing)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Frontend URL (for Stripe success/cancel redirects)
FRONTEND_URL=http://localhost:8080

# Stripe Price IDs (3 credit packs)
STRIPE_PRICE_ID_0_99_3=price_...
STRIPE_PRICE_ID_2_99_15=price_...
STRIPE_PRICE_ID_4_99_30=price_...

# Email (SMTP)
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_USE_SSL=true
EMAIL_ACCOUNT=no-reply@example.com
EMAIL_PW=your-email-password
EMAIL_FROM_NAME=PicPayGo

# Support email (for contact form)
SUPPORT_EMAIL=support@example.com
