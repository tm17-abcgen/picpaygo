# Shared infrastructure variables (used by docker-compose)
# See api/.env.example for API-specific variables

# PostgreSQL
POSTGRES_DB=picpaygo
POSTGRES_USER=picpaygo
POSTGRES_PASSWORD=picpaygo_dev

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
